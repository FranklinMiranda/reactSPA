import React from 'react';
import ReactDOM from 'react-dom';
import App from "./App"
import './index.css';

// Uses ReactDOM.render to render the source component App to the target node root
ReactDOM.render(<App />, document.getElementById('root'))

